{
  "name": "sdb-passkey",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w server\"",
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w client && npm run prepare:public -w server",
    "start": "npm run start -w server",
    "migrate:up": "npm run migrate:up -w server",
    "migrate:down": "npm run migrate:down -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
